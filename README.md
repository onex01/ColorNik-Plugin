# ColorNickPlugin v2.0.0

Плагин для изменения цвета ника игроков на сервере Minecraft с поддержкой Paper/Spigot.

## 🚀 Возможности

- **GUI интерфейс** для выбора цвета ника
- **Админ команды** для управления цветами других игроков
- **Консольная поддержка** для управления цветами
- **Блокировка цветов** - если админ установил цвет, игрок не может его изменить
- **Многоязычная поддержка** (русский и английский)
- **Сохранение цветов** между перезагрузками сервера
- **Отображение цветов** в чате, TAB-листе и над головой

## 📦 Установка

1. Скачайте `colornick-plugin-2.0.0.jar`
2. Поместите файл в папку `plugins/` вашего сервера
3. Перезапустите сервер
4. Готово!

## 🎨 Доступные цвета

- `red` - Красный
- `blue` - Синий
- `green` - Зеленый
- `yellow` - Желтый
- `aqua` - Голубой
- `light_purple` - Розовый
- `gold` - Золотой
- `gray` - Серый
- `white` - Белый
- `reset` - Сброс цвета

## 📋 Команды

### Для игроков:
```
/colornick  # Открыть меню выбора цвета
```

### Для администраторов:
```
/setcolornick <игрок> <цвет>  # Установить цвет игроку (блокирует изменение)
/unlockcolornick <игрок>     # Снять ограничение на изменение цвета
/colornick <игрок> <цвет>    # То же самое (альтернативный вариант)
/colornick <игрок> reset     # Сбросить цвет игрока
```

### Из консоли сервера:
```
colornick <игрок> <цвет>      # Установить цвет
colornick <игрок> reset       # Сбросить цвет
```

## 🔒 Разрешения

- `colornick.admin` - позволяет устанавливать цвета другим игрокам (по умолчанию OP)

## ⚙️ Настройка

### Основная конфигурация

Файл `config.yml` создается автоматически. Доступные настройки:

```yaml
# Язык интерфейса: ru (русский), en (английский)
language: ru
```

### Локализация

Плагин поддерживает несколько языков. Файлы локализации:
- `lang.yml` - текущие сообщения (создается автоматически)

**Поддерживаемые языки:**
- `ru` - Русский (по умолчанию)
- `en` - English

### Изменение языка

1. Откройте `plugins/ColorNickPlugin/config.yml`
2. Измените `language: ru` на `language: en`
3. Перезагрузите сервер

### Кастомизация сообщений

Вы можете редактировать сообщения в файле `plugins/ColorNickPlugin/lang.yml`

## 🔒 Особенности

- **Блокировка админских цветов**: Если администратор установил цвет игроку, тот не сможет изменить его самостоятельно через GUI
- **Сохранение данных**: Все цвета сохраняются между перезагрузками
- **Team система**: Цвета отображаются правильно в TAB-листе и над головой благодаря системе команд

## 🛠️ Технические детали

- **Java**: 21 LTS
- **Minecraft**: 1.21+
- **API**: Paper/Spigot
- **Зависимости**: Включены в JAR (shade)

## 📝 Примеры использования

```
# Админ устанавливает красный цвет игроку (блокирует изменение)
/setcolornick Steve red

# Игрок пытается изменить цвет (получит ошибку)
/colornick  # "Ваш цвет установлен администратором и не может быть изменен!"

# Админ снимает ограничение
/unlockcolornick Steve

# Теперь игрок может выбрать цвет сам
/colornick

# Админ сбрасывает цвет
/colornick Steve reset
```

### Конфигурация

```yaml
# config.yml
language: ru  # или en для английского
```
